# Dockers
```bash
mkdir docker
cd docker
```

install virtualenv
```bash
pip install virtualenv
virtualenv "docker"
```

Activate env
```bash
source docker/bin/activate
```

check docker env
```bash
docker hello-world
```

check docker version
```bash
docker --version
```

